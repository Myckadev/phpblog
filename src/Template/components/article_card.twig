<div class="card">
    <div class="card-body">
        <h5 class="card-title">{{ article.title }}</h5>
        <p class="card-text">{{ article.description }}</p>
        <a href="/phpblog/articles/{{ article.id }}" class="btn btn-primary">Voir plus</a>
    </div>
</div>